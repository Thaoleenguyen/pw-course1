**Note lesson 2 for JS**
1. For (i)
  for (start; condition; step) {
  // code chạy khi điều kiện đúng
}
_start → chạy 1 lần khi bắt đầu
condition → kiểm tra trước mỗi vòng
step → thực hiện sau mỗi vòng
<img width="544" height="319" alt="image" src="https://github.com/user-attachments/assets/ffc47a66-b448-484b-afac-b8ba2804fc2e" />
2. If
   if (điều_kiện) {
  // code chạy khi điều kiện đúng
}
VD : if (a > b) {
  console.log("a lớn hơn b");
}

